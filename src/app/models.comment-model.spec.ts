import { ModelsCommentModel } from './models.comment-model';

describe('ModelsCommentModel', () => {
  it('should create an instance', () => {
    expect(new ModelsCommentModel()).toBeTruthy();
  });
});
